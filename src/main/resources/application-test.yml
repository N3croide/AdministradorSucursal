spring:
  application:
    name: franchise-manager

  webflux:
    base-path: /api

  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true

server:
  port: 0
